18.04 LTS (64 bit) ubuntura
===========================

apt-get update

sudo apt-get install nano

sudo apt-get install mc

pdf kezelő eszközök
-------------------

sudo apt-get install poppler-utils

LAMP telepítése
---------------

sudo apt install tasksel

sudo tasksel install lamp-server

sudo nano etc/apache2/mods-enabled/dir.conf -ben index.php előre helyezése 

sudo service apache2 start

SSL telepítése
--------------
(előtte már domaint kellet rá irányitani)

sudo apt-get update
sudo apt-get install software-properties-common
sudo add-apt-repository universe
sudo add-apt-repository ppa:certbot/certbot
sudo apt-get update
sudo apt-get install certbot python-certbot-apache
-- kérdez email cimet, domain nevet, adatkezelés elfogadást
-- az /etc/letsescrypt -ben vannak a dolgai

FTP szerver telepítése
----------------------

sudo apt-get install vsftpd
sudo service vsftpd start  (az ftp klienssben sftp -t kell beállítani)
--- /etc/vsftp.conf -ban vannak a dolgai

phpmyadmin telepítése
---------------------

sudo apt install phpmyadmin php-mbstring php-gettext

-- létrehoz egy phpmyadmin mysql usert és kéri ennek jelszavát.-- 

.htaccess használat engedélyezése
---------------------------------
/etc/apache2/apache2.conf -ban:

<Directory /var/www/>
	Options Indexes FollowSymLinks
	AllowOverride All
	Require all granted
</Directory>

sudo service apache2 restart
sudo service vsftpd restart

uklogin afatbázis létrehozása phpmyadmin -al
--------------------------------------------

program feltöltése
------------------
(zip - unzip)
htaccess.txt -> .htaccess
config.txt --> .config.php

konfigurálás
------------
.config.php javitása


Sajnos a pdfsig jelenleg nem müködik (jelzett bug a github -on).
----------------------------------------------------------------
Alternativák:
1. mutols 
			
			openSSL tlepítés
			----------------
			
			sudo apt-get install openssl
			apt-cache search libssl | grep SSL
			
			mutools telepitése forrásból
			----------------------------
			
			forrás könyvtár  feltöltés (doc almappában benne van)
			cd forrás könyvtár
			make HAVE_X11=no HAVE_GLUT=no prefix=/usr/local install
			
			ha ez nem megy akkor prefix=~ ez utobbi esetben /root/bin mutool -al futtatható
			
			- sajnos a teszt szerveren OPNSSL hibával ez sem müködik
			
2.pdf-parser.py csinál valamit, kifejti az aláírást hexa formába obj 17 0  /sig résznél
  --- de ennek ellenörzése azt hiszem túl nagy falat lenne....


upgrade Ubunto 19.04 -re:
==========================
https://www.linuxbabe.com/ubuntu/upgrade-ubuntu-18-04-18-10-to-ubuntu-19-04

Ebben talán telepíthető a poppler-utils újabb verziója .....


ClientAliveInterval 60
sudo systemctl restart ssh
sudo apt update && sudo apt dist-upgrade
sudo apt install update-manager-core
sudo nano /etc/update-manager/release-upgrades
     At the bottom of this file, change the value of Prompt from lts to normal.

     Prompt=normal
do-release-upgrade

.......... több kérdés....... a végén reboot -ot csinál

verzió ellenörzés:


mail server telepités
=====================

https://www.linuxbabe.com/mail-server/ubuntu-18-04-iredmail-email-server


lsb_release -a 
    